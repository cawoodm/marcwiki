# Docker

## Docker Run
!! Make sure the image name is the last parameter, anything after it will be interpreted as a command
* Run `ls /` inside `alpine`: `docker run alpine ls /`
* Run alpine and get a command prompt: `docker run -it alpine`
* Run alpine in background (-d=detached): `docker run -d alpine`
sudo docker exec -ti [CONTAINER-ID] bash
RUN rabbitmq-plugins enable --offline rabbitmq_management